from django.utils.translation import gettext as _


class UserErrorMessages:
    EMAIL_REQUIRED = _("Users must have an email address.")
    USERNAME_REQUIRED = _("Users must have a username.")
